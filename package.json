{
  "name": "ai-knowledge-server",
  "version": "1.0.0",
  "description": "Admin and Public frontend for the AI Knowledge Base.",
  "main": "index.html",
  "scripts": {
    "build": "echo \"window.FIREBASE_CONFIG = ${FIREBASE_CONFIG}; window.GEMINI_API_KEY = '${GEMINI_API_KEY}'; window.ADMIN_PASSWORD = '${ADMIN_PASSWORD}';\" > config.js",
    "start": "serve -s .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
